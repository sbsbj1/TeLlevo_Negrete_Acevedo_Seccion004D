Pasos: npm i --save toast-controller npm install --save @ionic/storage

npm install --save @ionic/storage-angular

npm install @capacitor/google-maps npx cap sync

INSTALAR JSON: npm install -g json-server

DATA PASAJERO Y CONDUCTOR : json-server --watch pasajerox.json --host 0.0.0.0 --port 3000
json-server --watch conductor.json --host 0.0.0.0 --port 3000
